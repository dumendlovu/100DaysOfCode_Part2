doctype html
html
  head
    title First Pug Tutorial
  body
    input(type="password" name="InpPass" data-js='$(3 > 2 ? "OK" : "Not OK")')

    - const myClasses = ['class1','class2','class3'];
    div(class=myClasses)
    
    - const myStyles = {"color":"red", "background-color" : "blue"}
    div(style = myStyles)
    
    -const myAttri = {"src":"myphou.png", "alt":"This is my photo"}
    img&attributes(myAttri)
